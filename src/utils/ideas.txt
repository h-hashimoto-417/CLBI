1. 有方法调用的代码行更容易出错, 调用层次越多越容易出错 该行的出现的token数目可以表征方法调用的复杂性
2. 从前往后查看代码,在同一个方法中,分数相同的行应该按照行号进行排列
f1 = code location
f2 = token_number
f3 = call depth
f4 = call number
实证研究 bug在文件中的位置分布


RQ1 自身方法的性能
RQ2 与LINE_DP进行对比


调整为最差的序列时,会影响Simple模型的Recall,FAR等分类指标,因为Simple的分类结果是依赖于排序结果的
但是不会影响Supervised模型的分类指标,因为它不依赖于排序序列进行分类


易错点 函数体内的那些部件容易出错
1 函数调用 越多, 越复杂 越容易出错
2 条件判断处 分支条件 和 循环条件 表达式越复杂越容易出错, 影响程序流程的节点 for, while, if, else, switch, break, continue, return
3 函数返回处


＜日本語訳＞
1. メソッド呼び出しを含むコード行はエラーが発生しやすく、呼び出し階層が深くなるほどエラーリスクが高まる。当該行に現れるトークン数はメソッド呼び出しの複雑性を表す指標となる
2. コードを前方から順に確認する際、同一メソッド内でスコアが同値の行は行番号順に並べるべきである
f1 = コード位置
f2 = トークン数
f3 = 呼び出し深さ
f4 = 呼び出し回数
実証研究 バグのファイル内位置分布


RQ1 自身メソッドの性能
RQ2 LINE_DPとの比較


最悪のシーケンスに調整すると、SimpleモデルのRecallやFARなどの分類指標に影響する。Simpleの分類結果はソート結果に依存するためである。
しかしSupervisedモデルの分類指標には影響しない。分類がソートシーケンスに依存しないためである


エラー発生しやすい箇所 関数本体内の以下の部分がエラーを起こしやすい
1 関数呼び出し 呼び出し数が多いほど、複雑であるほどエラーが発生しやすい
2 条件分岐箇所 分岐条件やループ条件の式が複雑であるほどエラーが発生しやすく、プログラムフローに影響を与えるノード（for, while, if, else, switch, break, continue, return）
3 関数戻り値箇所

DeepL.com（無料版）で翻訳しました。